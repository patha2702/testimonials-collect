export interface MetricsWithIconProps {
  label: string
  icon: React.ReactNode
  value: number | string
}

export interface NavLink {
  id: number;
  label: string;
  href: string;
  icon: React.ReactNode;
}